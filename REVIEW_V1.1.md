# V1.1 代码审查报告

## 审查日期
2026-05-21

## 审查范围
V1.1-original 公告系统模块（管理员公告管理、学生公告查看）

---

## 发现的问题与修改

### 1. [健壮性] 公告内容多行输入缺少 EOF 检测
**问题**: 在发布和修改公告时，内容输入使用 `std::getline` 直接读取，没有检测 EOF（Ctrl+Z 或管道输入结束）。当输入意外结束时会导致程序行为异常。

**修改**: 在发布公告和修改公告的内容输入循环中增加了 `std::cin.eof()` 检测，检测到 EOF 时调用 `exit(0)` 优雅退出。

**影响文件**: `menu.cpp`（handleAdminAnnouncements 中的 case 2 和 case 3）

### 2. [功能缺失] 删除公告缺少确认步骤
**问题**: 管理员选择删除公告后直接执行删除操作，没有二次确认。误操作会导致公告数据不可恢复地丢失。

**修改**: 
- 删除前先通过 `findAnnouncement` 验证公告是否存在
- 显示公告标题并要求管理员确认（1-确认, 0-取消）
- 只有确认后才执行删除

**影响文件**: `menu.cpp`（handleAdminAnnouncements 中的 case 4）

## 已知限制（未修改）

- 公告内容中不能包含 `|` 字符（与用户数据相同的分隔符限制）
- 公告内容使用 "END" 作为输入结束标记，若内容中含有单独一行的 "END" 会被截断
- `menu.cpp` 文件持续增长，后续版本考虑拆分为多个 UI 文件

## 修改文件清单

| 文件 | 修改类型 | 说明 |
|------|----------|------|
| menu.cpp | 修改 | 内容输入增加 EOF 检测，删除增加确认步骤 |
