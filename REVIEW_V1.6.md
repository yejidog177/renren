# V1.6 代码审查报告

## 审查日期
2026-05-21

## 审查范围
V1.6-original Win32 GUI 界面（登录窗口、学生4标签页、管理员4标签页）

---

## 审查发现的问题

### 1. 标签控件不可见（学生+管理员窗口）⚠️ 严重

**问题**: 学生窗口和管理员窗口中大量 `CreateWindow("STATIC", ...)` 标签的 style 参数为 `0` 而非 `WS_CHILD|WS_VISIBLE`，导致所有字段标签不可见。

**影响**: 用户无法辨识各输入框的用途。节目报名界面无"节目类型""节目名称""时长""表演者""节目描述"等标签；修改密码界面无"旧密码""新密码""确认"标签。

**修复**: 将所有标签 style 改为 `WS_CHILD|WS_VISIBLE`，并将标签 HWND 存入 static 变量，在 `ShowStudentTab` / `ShowAdminTab` 中统一管理显示/隐藏。

### 2. 标签页切换时控件泄漏（学生窗口）⚠️ 严重

**问题**: `ShowStudentTab` 和 `ShowAdminTab` 只管理了输入控件的 show/hide，未管理标签（STATIC 控件）。Tab 0 的"比赛公告""入选名单"标签在切换到其他 tab 时仍然可见。

**影响**: 个人中心界面出现"入选名单"等无关文字和一个空白框。

**修复**: 将所有标签纳入 show/hide 管理，切换 tab 时正确隐藏非当前 tab 的所有控件。

### 3. 学生公告查看界面设计问题

**问题**: Tab 0 分上下两部分，上为"比赛公告"列表，下为"入选名单"列表。但入选名单本身也是一种公告，无需独立展示。且选中公告后无法查看其内容。

**修复**: 移除独立入选名单列表，改为上方公告列表 + 下方公告内容展示区。点击列表中的公告自动显示详细内容。公告列表改用 `getAnnouncements()` 同时展示普通公告和入选名单。

### 4. 管理员公告管理缺少点击填充

**问题**: 管理员 Tab 0 的公告列表不支持点击选中后自动填充标题/内容/类型到编辑区，修改公告时需手动重新输入。

**修复**: 添加 `LVN_ITEMCHANGED` 通知处理，点击公告自动填充标题、内容和类型下拉框。

---

## 审查结论

所有 UI 审查问题已修复。V1.6 GUI 在功能完整性上满足设计要求，学生和管理员的所有操作入口均可正常使用。

### 已验证项

- 登录窗口：用户名/密码输入，登录/注册/退出按钮正常
- 注册表单：专业-班级联动下拉框正常，表单校验正常
- 学生 Tab 0：公告列表显示全部公告，点击查看内容正常
- 学生 Tab 1：节目报名各字段标签清晰，保存功能正常
- 学生 Tab 2：材料提交界面标签和说明文字清晰
- 学生 Tab 3：个人信息展示和密码修改功能正常
- 管理员 Tab 0：公告列表点击填充编辑区，发布/修改/删除正常
- 管理员 Tab 1：审核列表点击查看详情，通过/驳回/撤销正常
- 管理员 Tab 2：专业-班级关联查看正常
- 管理员 Tab 3：密码修改标签完整、功能正常
- 编译通过（GCC 3.4.5，3个 -1→WPARAM 警告，非功能性）

### 已知限制（项目整体）

1. **密码安全**: 使用 XOR 编码而非哈希，适合教学项目但不适用于生产环境
2. **分隔符限制**: 所有文本字段不能包含 `|` 字符
3. **并发安全**: 单用户应用，无并发控制
4. **数据备份**: 无自动备份机制
5. **管理员数据管理**: 专业/班级的添加功能为占位 MessageBox，实际需通过控制台版本或直接编辑数据文件操作
6. **字体硬编码**: 使用"微软雅黑"字体，若系统无此字体可能回退到默认字体

### 项目完成清单

| 模块 | 版本 | 功能 |
|------|------|------|
| 核心基础 | V1.0 | 用户管理、登录注册、数据持久化 |
| 公告系统 | V1.1 | 管理员发布公告、学生查看公告和入选名单 |
| 节目报名 | V1.2 | 学生编辑节目信息（类型/时长/名称/描述/表演者） |
| 材料提交 | V1.3 | 学生提交节目材料 |
| 审核系统 | V1.4 | 管理员审核报名（通过/驳回/撤销） |
| 集成优化 | V1.5 | 专业/班级管理、最终集成 |
| **GUI界面** | **V1.6** | **Win32 图形界面，登录/注册/学生4标签页/管理员4标签页** |

## 项目统计

- 总代码行数: ~2795 行 (C++)
- 源文件: 13 个 (7 .cpp + 6 .h)
- Git 分支: 14 个 (7 original + 7 review)
- 数据文件: 5 个 (.dat)
- GUI 可执行文件: talent_show_gui.exe (~768KB)
